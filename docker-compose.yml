version: '3'
services:
  go:
    container_name: go
    build:
      context: go
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./go/src:/go/src:cached
    command:
      godo server --watch
    tty: true
    stdin_open: true
